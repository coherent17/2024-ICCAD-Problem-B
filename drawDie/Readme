usage: drawDie -i [Input_Name] -m [Image_Name] -t [Image_Title] [-g -o -p -nl -c cellname_file -n net_file]

A script that processes input file or cell list file & net list file to generates a chip image.

options:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Input file path
  -m IMAGES, --images IMAGES
                        Images name
  -t TITLE, --title TITLE
                        Images title
  -g, --grid            Not draw bin line and placement row on image
  -o, --overlap         Not draw overlap region on image
  -p, --pin             Not draw pin on image
  -nl, --netlist        Not draw netlist on image
  -c CELL, --cell CELL  Read cell list file and draw unique cell on image
  -n NET, --net NET     Read net list file and draw unique net on image


